<div class="sipz-welcome-login">
  <p>
    Share</br>
    - beers</br>
    - breweries</br>
    Rate and fallow</br>
    - beers</br>
    - breweries</br>
    - users
  </p>
</div>
<div class="sipz-form-login">
  <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= f.input :email, required: true, label: false, placeholder: 'email', class: 'btn' %>
    <%= f.button :submit, 'Send reset password instructions', class: 'sipz-enter float-right' %>
  <% end %>
</div>